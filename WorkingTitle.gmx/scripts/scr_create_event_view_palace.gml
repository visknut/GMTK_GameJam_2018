/// scr_create_event_view_palace()
/*
var _location;
var _palace;

if (instance_number(obj_palace > 0))
{
    
}

// Find location of palace.
if (_palace.x > 360 && _palace.y < 272)
{
    _location = NORTH_EAST;
} else if (_palace.x < 360 && _palace.y < 272)
{
    _location = NORTH_WEST;
} else if (_palace.x > 360 && _palace.y > 272)
{
    _location = SOUTH_EAST;
} else 
{
    _location = SOUTH_WEST;
}

var _new_event = instance_create(0, 0, struct_event_view_palace);
_new_event.location = _location;

_new_event.message = scr_enqueue_message(CHAR_KING, "TODO: event_view_palace");

with (obj_kingdom)
{
    ds_list_add(events, _new_event);
}
*/
// FOLOW UP!
